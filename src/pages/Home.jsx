import { Typography } from "@mui/material";

const Home = () => {
  return (
    <Typography variant="h6" sx={{ my: 1, color: "#656565" }}>
      Hi, Welcome back!
    </Typography>
  );
};

export default Home;
